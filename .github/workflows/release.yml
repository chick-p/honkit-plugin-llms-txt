name: Run Tagging and Release

on:
  push:
    branches:
      - main
    paths:
      - "package.json"

permissions:
  contents: write

jobs:
  call:
    uses: chick-p/github-actions-shared-workflow/.github/workflows/release-npm-package-auto.yml@19278e10d4e7f1860a5ff184e9ddc622bba230f3 # v0.4.0
